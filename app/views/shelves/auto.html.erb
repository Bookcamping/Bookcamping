<div id="autoshelves">
  <% AutoShelf::NAMES.each do |name| %>
      <%= link_to t("autoshelf.name.#{name}"), autoshelf_path(name), :class => name %>
  <% end %>
</div>
<br/>
<%= link_to 'Volver', root_path, :class => 'back' %>
<br/>
<%= render_books autoshelf.books, '#efefef' %>

<%= content_for(:sidebar) { render 'auto_sidebar' } %>
