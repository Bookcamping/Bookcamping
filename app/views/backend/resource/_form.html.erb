<%= simple_form_for [:backend, resource], html: {class: 'form-stacked'} do |f| %>
    <% resource_attributes.each do |attr| %>
        <%= f.input attr %>
    <% end %>
    <%= f.submit 'Guardar' %>
<% end %>
