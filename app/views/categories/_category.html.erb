<% if can? :view, category %>
    <h2><%= category.name %></h2>
    <p class="hint"><%= category.description %></p>
    <table class="table">
      <tbody>
      <% category.pages.each do |page| %>
          <tr>
            <td><%= link_to page.title, page %></td>
            <td>hace <%= time_ago_in_words page.updated_at %></td>
            <td class="actions">
              <%= link_to 'Editar', [:edit, page] if can? :edit, page %>
            </td>
          </tr>
      <% end %>
      </tbody>
    </table>
<% end %>
