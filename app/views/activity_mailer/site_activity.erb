<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type"/>
</head>
<body>
<h1>Actividad en #bookcamping</h1>

<p>Desde el <%= l @last_email_at %> (<%= time_ago_in_words @last_email_at %>)<br/>
hasta el <%= l Time.now %> (ahora mismito)</p>
<% @versions.each do |version| %>
    <p>
      <%= link_to version.user_name, user_url(version.whodunnit) %> ha
      <%= t("versions.events.#{version.event}") %> la
      <%= t("versions.models.#{version.item_type}") %>
      <% title = version.title ? version.title : '<sin título>' %>
      '<%= link_to truncate(title, length: 50), version_url(version) %>'
      <small>hace <%= time_ago_in_words version.created_at if version.created_at %></small>
    </p>
<% end %>
</body>
</html>
