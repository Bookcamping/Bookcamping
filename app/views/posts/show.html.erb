<div class="actions">
  <%= link_to 'Volver', root_path, class: 'back' %>
  <%= link_to 'Administrar blog', admin_posts_path if can? :manage, post %>
  <%= link_to 'Editar entrada', [:edit, :admin, post] if can? :manage, post %>
</div>

<%= render :partial => 'show' %>

<%= content_for(:sidebar) { render 'sidebar'} %>